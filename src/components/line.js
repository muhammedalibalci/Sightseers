import React from 'react'
import { View, StyleSheet } from 'react-native'

export const Line = () => {
    return (
        <View style={{ borderBottomWidth: StyleSheet.hairlineWidth, borderColor: 'gray' }} />
    )
}
